#pragma once

//void print_results(const size_t& max_blocks, std::vector<int>& bytes, std::vector<int>& results, std::vector<int>& lines);

//void split_to_blocks(Block& block, const size_t& max_blocks, size_t& leftover, int& counter, std::vector<Block>& blocks, int& retflag);
